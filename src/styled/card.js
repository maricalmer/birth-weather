import styled from 'styled-components';

const Card = styled.div`
	background-color: rgba(150, 167, 181, .5);
	border-radius: 24px;
	width: 650px;
	height: 375px;
	padding: 3rem;
	position: relative;
	overflow: hidden;
`;

export default Card;
